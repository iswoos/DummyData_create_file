package com.example.orderinformation.service;

import com.example.orderinformation.dto.ResponseDto;
import org.springframework.stereotype.Service;

@Service
public class OrderInfoService {


//    public ResponseDto<?> findOneOrderInfo() {

        // OrderInfo (연관관계를 맺지 않은)
        // Account_id, Product_id

        // Account_id
//      accountRepository.findById(Account_id)

//      OrderInfoRepository(Account account, name)

//      OrderInfo.account.getName()
        // inner JOIN
        // Account id == OrderInfo Account ID

        // [[ JOIN ]]
        // JOIN ==> 검색 성능 대상이 생긴다.

        //


//    }

}
